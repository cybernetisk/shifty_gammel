<h1>Listing username</h1>

<%= render 'nav' %>

<div class="box left_div">
  <h2>Du er administrator i følgende grupper:</h2>
</div>

<div class="box left_div">
  <h2><%= t "users.find" %></h2>
  <input name="user_search" id="user_search" type="text" />
  <ul id="user_search_results">
    <li><%= t "users.type_to_search" %></li>
  </ul>
</div>

<table>
  <tr>
    <th>Username</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= link_to 'Groups', "users/#{user.id}/groups" %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>
