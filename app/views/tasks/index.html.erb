<h1>Listing tasks</h1>

<table>
  <tr>
    <th>User</th>
    <th>Description</th>
    <th>Title</th>
    <th>User group</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.user_id %></td>
    <td><%= task.description %></td>
    <td><%= task.title %></td>
    <td><%= task.user_group_id %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
